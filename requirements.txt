# Glimmer v0.1 — core dependencies
pyyaml>=6.0
numpy>=1.24
matplotlib>=3.7
networkx>=3.0
# Optional: requests (for the reference agent calling out to LLM APIs)
# Optional: bids-validator (for `glimmer validate --include-bids` in v0.2)
